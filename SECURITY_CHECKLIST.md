# セキュリティ監査チェックリスト（Naritai-APP）

このサイトは **GitHub 上で公開されているコーポレートサイト**（Next.js / Vercel 自動デプロイ）。
ログイン機能もデータベースも無く、扱う個人情報は問い合わせフォームの入力値だけ、という前提で書いている。

## 使い方

1. 開発に使ったのとは別の AI に、次の一文を投げる。
   > このリポジトリを `SECURITY_CHECKLIST.md` の観点で、攻撃者の目線で監査してください。
   > 各項目について「合格 / 要修正 / 対象外」を判定し、**実際のファイル名と行番号を根拠として**示してください。
   > 推測で書かず、確認できなかった項目は「未確認」と書いてください。
2. 出てきた指摘を、下の「監査ログ」に日付つきで追記する。
3. 修正を入れたら、次回の監査で直っているかを再確認する。

---

## 1. 認証・認可【開発時】

現状は**対象外**。ログイン機能も、ユーザーごとに守るべきデータも存在しない。

ただし次のどれかを足した瞬間に、この項目は最優先になる：

- 管理画面・お知らせ（`src/lib/news.ts`）の入稿画面
- 会員登録やマイページ
- フォーム送信内容を保存するデータベース

そのときは「ログインさせる（認証）」だけで終わらせず、
**「ログイン中の人が、その ID のデータを見てよいか」を毎回サーバー側で確認する（認可）**こと。
URL の ID を書き換えれば他人のデータが見える、が最も多い事故。

## 2. シークレットの外部公開【開発時】

**このリポジトリは Public。** push した瞬間に全世界から読める。

見るポイント：

- キーがコードに直書きされていないか
- `.env` 系が追跡されていないか、**過去のコミットにも入っていないか**
- `NEXT_PUBLIC_` が付いた環境変数に、秘密にすべき値が入っていないか

```bash
git ls-files | grep -iE '\.env|secret|credential|key'
git log --all --name-only --pretty=format: | sort -u | grep -iE '\.env'
git log --all -p | grep -inE 'sk-[A-Za-z0-9]{16,}|AIza[0-9A-Za-z_-]{20,}|ghp_[A-Za-z0-9]{20,}'
grep -rno 'process\.env\.[A-Z_]*' src/ | sort -u
```

合格基準：直書きゼロ、履歴にもゼロ、`NEXT_PUBLIC_` が付くのは公開されても困らない値だけ。
**`NEXT_PUBLIC_` はブラウザに配信されるコードに埋め込まれ、開発者ツールで誰でも読める。**
サーバー側でしか使わない値には付けないこと。
一度でも push したキーは、あとで消しても漏れたものとして扱い、必ず再発行する。

### EmailJS について

`NEXT_PUBLIC_EMAILJS_*`（`src/app/contact/page.tsx`, `src/app/blank/page.tsx`）が
ブラウザから見えるのは **EmailJS の仕様どおりで、漏洩ではない**。
ただしキーを抜いた第三者が勝手に送信できるため、防御は EmailJS 管理画面側で行う：

- 許可ドメインの制限（自社ドメインのみ）
- レート制限
- reCAPTCHA など、フォームのボット対策

合格基準：上の 3 つが EmailJS 側で設定されていること。**コードを見ても分からないので、管理画面で確認する。**

## 3. ライブラリのバージョン【開発時】

```bash
npm install
npm audit                # 脆弱性
npm audit --omit=dev     # うち、本番で動くコードに含まれるもの
npm outdated             # 古さ
```

見るポイント：

- `next` のメジャーバージョン。**Next.js は最新 2 メジャーしかセキュリティ修正が出ない**ので、
  そこから外れたら EOL 扱いになる
- `npm audit --omit=dev` に出るものは、閲覧者に届くコードの問題なので優先度が高い
- dev 側だけの指摘は、ビルドする自分の PC が対象。優先度は下がるが放置はしない

合格基準：`npm audit` が 0 件、`next` がサポート内のメジャー。

## 4. ログ【運用時】

サーバーサイドの処理がほぼ無いため、アプリ側の実装は**対象外**。
アクセスログは Vercel 側に残る。

将来 Route Handler（`src/app/api/...`）や DB を足したら、この項目を有効化して
「誰がいつ何をしたか」を記録する。**フォームの入力値そのものをログに出さないこと**（個人情報が残る）。

## 5. 依存の継続監視【運用時】

- GitHub リポジトリの Settings → Code security で **Dependabot alerts** を有効にする
- `.github/dependabot.yml` を置くと、更新の PR まで自動で作られる（`package-ecosystem: npm`）
- **`main` への push がそのまま本番反映**される設定なので、Dependabot の PR は
  内容を確認してからマージする（自動マージにしない）

合格基準：アラートが有効で、未対応のアラートが残っていないこと。

---

## 監査ログ

### 2026-08-21（Claude Code / 初回）

| # | 項目 | 判定 | 内容 |
|---|---|---|---|
| 1 | 認証・認可 | 対象外 | ログイン機能・ユーザーデータとも無し |
| 2 | シークレット | **合格** | 全コミット履歴を走査してキー・秘密鍵の混入ゼロ。`.env` 未追跡。過去の `.env.example` もプレースホルダのみ。`NEXT_PUBLIC_*` は EmailJS / GA / GTM / BASE_URL で、いずれも公開前提の値。※ EmailJS 管理画面側のドメイン制限・レート制限・ボット対策は**未確認** |
| 3 | ライブラリ | **要修正** | `npm audit` で 14 件（high 13 / moderate 1）。うち本番依存は 6 件（vite / rollup 系、`npm audit fix` で解消可）。`next` が 14.2.33 で、最新は 16.3.1 → **Next.js 14 はセキュリティ修正の対象外**。`@next/third-parties: ^16` と `next: ^14` のメジャーがずれている |
| 4 | ログ | 対象外 | サーバーサイド処理が無く、アクセスログは Vercel 側 |
| 5 | Dependabot | **要修正** | `.github/` が無く未設定。**公開リポジトリなので優先度が高い** |

### 2026-08-21（修正記録）

| # | 項目 | 判定 | 対応内容 |
|---|---|---|---|
| 3 | ライブラリ | 要修正（変更なし） | `npm audit` 14 件・`next` 14 系のまま。Next 16 への更新は表示崩れの確認が要るため未着手 |
| 5 | Dependabot | **一部修正** | `.github/dependabot.yml` を追加（npm）。メジャー更新は個別 PR になるよう設定。**GitHub の Settings → Code security で Dependabot alerts を有効にする操作が別途必要** |
